{% extends 'base.html.twig' %}

{% block title %}Edit Job{% endblock %}

{% block body %}
    {% if app.user %}
    <h1>Edit Job</h1>

    {{ include('job/_form.html.twig', {'button_label': 'Save'}) }}

    {{ include('job/_delete_form.html.twig') }}

    {% else %}
    <a href="{{ path('app_login') }}">Unauthorized access. Please, login.</a>
    {% endif %}
{% endblock %}
 